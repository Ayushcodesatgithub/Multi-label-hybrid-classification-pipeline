# Multi-label-hybrid-classification-pipeline
concatenating the 384 MiniLM features with the 200 TF-IDF features, you give the XGBoost model the best of both worlds, enabling it to achieve better prediction accuracy than either feature set could achieve alone
